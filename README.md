dataset = NSL_KDD

Installer :  
pip install pandas numpy seaborn matplotlib scikit-learn  
pip install scapy  
pip install scrapy

Lancé : _python3 dataset_cleaning_model_training.py_   
Pour générer un fichier model_RF.pkl   
Puis lancé streamlit :  _sudo streamlit run streamlit.py_  
En tant qu'admin pour les accès de scapy

Pour l'instant le scan des paquets depuis le terminal avec traitement 
de l'ia en menace ou non, affichage streamlit avec trie des paquets 
selon la menace n'est pas encore fonctionnelle.

